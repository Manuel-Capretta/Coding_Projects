public class dog extends animals {
    public dog(String name){
        super(name); //call for name from animals class and set it to constructors variable
        noise = "bark"; //set noise to standard noise for every dog animal
    }
}
